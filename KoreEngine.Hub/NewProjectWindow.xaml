<Window x:Class="KoreEngine.Hub.NewProjectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Nouveau projet KoreEngine" Height="296" Width="480"
        Background="{DynamicResource BackgroundBrush}"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="False"
        SourceInitialized="Window_SourceInitialized">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36" CornerRadius="0" GlassFrameThickness="0"
                             ResizeBorderThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="{DynamicResource SidebarBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Nouveau projet" Foreground="{DynamicResource MutedTextBrush}"
                       FontSize="12" VerticalAlignment="Center" Margin="14,0,0,0"/>
            <Button Grid.Column="1" Content="&#xE8BB;" Style="{StaticResource TitleBarCloseButton}"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="Cancel_Click"/>
        </Grid>

        <StackPanel Grid.Row="1" Margin="20">
            <TextBlock Text="Nom du projet" Foreground="{DynamicResource MutedTextBrush}" Margin="0,0,0,4"/>
            <TextBox x:Name="ProjectNameBox" Style="{StaticResource RoundedTextBox}" Padding="6" Margin="0,0,0,16"/>

            <TextBlock Text="Emplacement" Foreground="{DynamicResource MutedTextBrush}" Margin="0,0,0,4"/>
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="LocationBox" Style="{StaticResource RoundedTextBox}" Padding="6" IsReadOnly="True"/>
                <Button Grid.Column="1" Content="Parcourir..." Margin="8,0,0,0" Padding="10,6" Click="Browse_Click"/>
            </Grid>

            <TextBlock x:Name="ErrorText" Foreground="#E85555" TextWrapping="Wrap" Margin="0,0,0,12"
                       Visibility="Collapsed"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Annuler" Padding="14,6" Margin="0,0,8,0" Click="Cancel_Click"/>
                <Button Content="Créer" Padding="14,6"
                        Background="{DynamicResource AccentBrush}" Foreground="White" BorderThickness="0"
                        Click="Create_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
