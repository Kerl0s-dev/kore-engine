<Window x:Class="KoreEngine.Hub.BuildLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Build en cours..." Height="456" Width="640"
        Background="{DynamicResource BackgroundBrush}"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="False" ResizeMode="CanResizeWithGrip"
        SourceInitialized="Window_SourceInitialized">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36" CornerRadius="0" GlassFrameThickness="0"
                             ResizeBorderThickness="6" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="{DynamicResource SidebarBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="TitleBarText" Grid.Column="0" Text="Build en cours..." Foreground="{DynamicResource MutedTextBrush}"
                       FontSize="12" VerticalAlignment="Center" Margin="14,0,0,0"/>
            <Button Grid.Column="1" Content="&#xE8BB;" Style="{StaticResource TitleBarCloseButton}"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="Close_Click"/>
        </Grid>

        <Grid Grid.Row="1" Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="StatusText" Grid.Row="0" Text="Nettoyage puis build en cours..."
                       Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold"
                       Margin="0,0,0,8"/>

            <ScrollViewer Grid.Row="1" x:Name="LogScroll" VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="LogBox" IsReadOnly="True" TextWrapping="NoWrap"
                         FontFamily="Consolas" FontSize="12"
                         Style="{StaticResource RoundedTextBox}"/>
            </ScrollViewer>

            <Button Grid.Row="2" Content="Fermer" HorizontalAlignment="Right" Margin="0,8,0,0"
                    Padding="14,6" Click="Close_Click"/>
        </Grid>
    </Grid>
</Window>
